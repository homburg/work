// Cloudflare Worker serving Just Paws as static assets.
// Deployed by Workers Builds on push to main (root directory: apps/just-paws).
{
  "name": "just-paws",
  "compatibility_date": "2026-09-25",
  "assets": {
    "directory": "."
  }
}
